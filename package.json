{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start:server": "json-server-auth --watch db.json --port 3001",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "axios": "^0.26.0",
    "navigo": "^8.11.1"
  }
}
